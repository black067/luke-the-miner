{
  "name": "pinball-html",
  "version": "1.0.0",
  "description": "星环矿工",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "del /q dist\\js 2>nul || rmdir /s /q dist\\js 2>nul || true",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
